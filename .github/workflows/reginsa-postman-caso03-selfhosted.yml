name: REGINSA Postman Caso 03 (Self-hosted)

on:
  workflow_dispatch:
    inputs:
      upload_artifacts:
        description: "Upload artifacts (true/false)"
        required: false
        default: "true"

jobs:
  run:
    uses: ./.github/workflows/reusable-postman-caso-selfhosted.yml
    with:
      caso: "03"
      upload_artifacts: ${{ github.event.inputs.upload_artifacts || 'true' }}
    secrets: inherit
